// COPY THIS FILE TO `config.js` AND SET YOUR APPS SCRIPT URL
// Example:
// window.__CONFIG__ = { WEB_APP_URL: 'https://script.google.com/macros/s/XXXXX/exec' };

window.__CONFIG__ = { WEB_APP_URL: '' };

// Note: For security, do NOT commit a `config.js` with real credentials/URLs
// if you prefer to keep the backend URL secret. Instead, set it via Vercel
// environment variables and use a build step to inject it.
